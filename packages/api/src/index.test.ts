import { describe, it } from 'vitest'

describe('API Package', () => {
  it('should exist', () => {
    // Simple placeholder test to ensure the package is testable
    // Add real tests as the API package grows
  })
})
